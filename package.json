{
  "name": "canvas-scraper",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node index.js",
    "tui": "node index.js --tui",
    "start": "./canvas-scraper.exe",
    "test": "node --test",
    "build-win": "nexe index.js -o dist/canvas-scraper-win.exe --build --verbose",
    "build-linux": "nexe index.js -o dist/canvas-scraper-linux --build --verbose",
    "build-mac": "nexe index.js -o dist/canvas-scraper-mac --build --verbose",
    "build": "npm run build-win && npm run build-linux && npm run build-mac"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "nexe": "4.0.0-rc.7"
  },
  "dependencies": {
    "commander": "^11.1.0",
    "dotenv-flow": "^4.1.0",
    "ink": "^7.1.1",
    "inquirer": "^9.2.23",
    "p-limit": "^5.0.0",
    "puppeteer": "^21.9.0",
    "react": "^19.2.8"
  }
}
